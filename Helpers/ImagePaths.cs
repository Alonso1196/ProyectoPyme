using System;

namespace ProyectoPyme.Helpers
{
    public static class ImagePaths
    {
        // Use ~ so Url.Content can resolve to the application root
        public const string Productos = "~/images/Productos";
    }
}
